## 📊 Manifest Changes

{{if .ManifestChanges}}
{{range $env, $diff := .ManifestChanges}}

### [`{{$env}}`]: {{if gt $diff.LineCount 0}}`{{$diff.LineCount}}` lines ({{$diff.AddedLineCount}}➕/{{$diff.DeletedLineCount}}➖){{else}}No changes detected.{{end}}

{{if gt $diff.LineCount 0}}
{{if eq $diff.ContentType "ext_ghartifact"}}
📎 **Diff too large to display inline.**  
View the full diff in the artifact: [`{{$diff.Content}}`](../../actions/runs/${{github.run_id}})

> **Note**: The diff contains {{$diff.LineCount}} lines and has been uploaded as an artifact for easier review.
{{else}}
<details open>
<summary>Click to expand {{$env}} diff</summary>

```diff
{{$diff.Content}}
```

</details>
{{end}}
{{else}}
✅ No changes detected.
{{end}}

{{end}}
{{else}}
✅ No changes detected.
{{end}}